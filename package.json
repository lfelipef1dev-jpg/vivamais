{
  "name": "vivamais",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx serve out",
    "test": "playwright test",
    "lint": "eslint src build.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "clean-css": "^5.3.3",
    "eslint": "^10.10.0",
    "globals": "^17.12.0",
    "terser": "^5.51.2"
  }
}
